# 未決の論点

決まっていないと気づいた時点で追記する。決まったら decisions.md にエントリを
足し、ここの状態を更新する（解決済みの項目は消さず、決定へのポインタを残す）。

## オーナー以外に同じ痛みがあるか

- 何が決まれば決まるか: オーナー以外の英語学習者にも「チャットで一言返せない」痛みがあるかの裏付け。
- 材料: いまのところオーナー自身の観測のみ。最初の想定ユーザーはオーナー自身。2026-09-15 オーナーが自分が唯一のユーザーと明言。初版では扱わない。
- 状態: 保留

## 採点の観点セットと尺度

- 何が決まれば決まるか: 文法・語彙・自然さ・分かりやすさのどれを、何段階で、各段階の記述をどう固定するか。
- 材料: 100 点満点だと天井にすぐ届く懸念（オーナー）。CEFR のような段階に対応させれば天井が遠いという案あり。観点を後から変えると過去と比べられなくなるので、記録を持つ前に固定する必要がある。research/scoring-rubrics.md。
- 状態: 決定済み（decisions.md 2026-09-15「採点の観点は 4 つ」「尺度は下位項目 8 つを 0〜5」）

## ダッシュボードで見せる項目

- 何が決まれば決まるか: 直近の傾向・多い間違い・覚える単語・次に意識すること、を構造化した項目で見せるか、ざっくり全体像で見せるか。
- 材料: オーナーが助言を求めている段階。
- 状態: 初版は決定済み（全体像の文章。decisions.md 2026-09-15「ダッシュボードは初版はざっくり全体像の文章」）。構造化は将来の論点として残る。

## 「セッション」の単位

- 何が決まれば決まるか: 1 問ごとに採点・記録するか、N 問をひとまとまりにするか。連続反復と記録の粒度に関わる。
- 材料: グローバル skill building-chat-uis の session-architecture（セッションの境界を先に決める、採点はセッション終了時に 1 回、合否の閾値はアプリ側、結果画面は吹き出しの続きにしない）。
- 状態: 決定済み（1 問単位。decisions.md 2026-09-15「セッションの単位は 1 問。採点は 1 問ごと」）

## 質問の出所と場面の配分

- 何が決まれば決まるか: 固定リストか LLM 生成か。場面（友人 / SNS・コミュニティ / 仕事）をどう混ぜるか。
- 材料: ブラウザ内保存しかないので出題済みの管理は限定的。組み合わせ表（相手・場面・話題の候補）の中身は実装側で作る。decisions.md「AI 構成は固定ワークフロー」。
- 状態: 実装側へ。（LLM 生成 + 内部の組み合わせという方式は決定済み。decisions.md 2026-09-15「質問は毎回 LLM 生成」「AI 構成は固定ワークフロー」）

## 「何を返したらいいか分からない（母語でも悩む）」の扱い

- 何が決まれば決まるか: いつ・どう扱うか。english-opinion-trainer の (c) と同じ問題。
- 材料: 前回はここにタネを足して複雑になった。今回は対象外にしたが、「こう返しましょうと指定されるのも悩ましい」というオーナーの言葉があり、扱い方が未定。
- 状態: 未決

## 既存プロダクトに時間制限つきの 1 行返信ドリルがあるか

- 何が決まれば決まるか: 事実確認。あれば作る理由が弱くなる。
- 材料: なし。
- 状態: 却下（調べない。decisions.md 2026-09-15「競合調査はしない」）

## 30 秒という数字の妥当性

- 何が決まれば決まるか: 実際に使って決める。
- 材料: 名前に入れていないので変えられる。
- 状態: 未決

## 採点呼び出しの応答速度

- 何が決まれば決まるか: 30 秒の返答直後に何秒待たせるか。
- 材料: assessing-ai-architecture の応答速度の軸で扱う。次の質問を採点中に先に生成しておく案あり。decisions.md 2026-09-15「ベンダーとモデル」で採点は high で開始。landscape 2026-09-15 の実測では high は medium の約 2 倍の待ち時間。使いながら直す。
- 状態: 実装後に使いながら判断。

## ブラウザ内保存の手段と Cookie 同意

- 何が決まれば決まるか: localStorage / Cookie などの技術的な手段。
- 材料: 技術スタックはオーナーが「任せる」と言ったので shaping-experience / assessing-ai-architecture で決める。Cookie を使うなら同意ボタンが要る（オーナーの申し送り）。採点基準のバージョンを記録に刻む必要がある（基準を変えたら比較できない）。各記録にモデル別名・推論強度・採点基準のバージョンを刻む（research/luna-api-constraints.md）。
- 状態: 未決

## 将来版の扱い

- 何が決まれば決まるか: 複数往復、STAR 等の構造指定つき長めモード（3〜5 分）、単語学習機能の統合（english-vocab-app 由来、あちらは捨てる可能性あり）、外部 DB への記録を、いつどう入れるか。
- 材料: 初版の後で検討。
- 状態: 保留

## 「前回と比べたコメント」の別呼び出し

- 何が決まれば決まるか: 採点とは別の呼び出しで過去を見せてコメントさせるかどうか。
- 材料: decisions.md「採点は過去を見せず単発。比較はアプリ側」参照。記録を持つ版で再検討。
- 状態: 保留

## PC で座ってやる習慣が続くか

- 何が決まれば決まるか: 実際に数日使って続くか。
- 材料: スマホ隙間時間が当初の推奨だったが初版はローカルのため PC。続かなければ公開を早める。
- 状態: 未決

## 8 項目 × 6 段階の記述を誰がどう書くか

- 何が決まれば決まるか: 48 個の記述の作り方（人手で書く / LLM に下書きさせてオーナーが直す）と、書いた記述をどう検証するか（同じ返答を複数回採点して揺れを見る、など）。
- 材料: research/scoring-rubrics.md（既存ルーブリックはメール・投稿向けで 1〜2 文には流用できない）。decisions.md「実装時の人手検証を計画に入れない」により、AI が下書きしてそのまま使い、気になったら直す。検証手順は置かない。
- 状態: 実装側へ。

## 重み付け

- 何が決まれば決まるか: 4 観点の重みを均等のままにするか。
- 材料: アプリ側の数値なので後から変えても過去の採点は残る。
- 状態: 未決（使ってから）

## 公開時の鍵とサブスク課金

- 何が決まれば決まるか: Vercel に置くときの簡易な鍵の方式と、Stripe 等の課金をいつ入れるか。
- 材料: 当分は全体公開しない、とオーナー。
- 状態: 保留

## モデル版の固定と採点の再現性

- 何が決まれば決まるか: スナップショットで版を固定できない場合に、採点の連続性をどう担保するか。温度が指定できない場合の代替。
- 材料: research/luna-api-constraints.md（日付つきスナップショットは公開ドキュメント上なし。temperature の可否は未確認で、reasoning モデルでは無視・拒否の傍証あり）。
- 推奨: 各記録にモデル別名・推論強度・採点基準バージョンを刻み、再現性は「記述で固定した尺度 + 推論強度の固定」に頼る。温度は指定できれば低くする程度に留める。
- 状態: 未決(実装側で扱う)。

## AI SDK 実装上の注意

- 何が決まれば決まるか: 実装時に守る点の確認。
- 材料: research/luna-api-constraints.md（Zod の `.optional()` は使えず `.nullable()`。強度は `providerOptions.openai.reasoningEffort`。strict は既定で有効）。`docs/landscape/2026-09-chat-ui-stack.md`（v7 の API 名。古い snippet の見分け方）。
- 状態: 実装側へ。
