# 決定の記録

追記のみ。過去のエントリは書き換えない。新しいものを下に足していく。
決まったら open-questions.md の該当論点の状態も更新する。

2026-09-07 のセッションでは、途中まで「語彙リストのライブラリ」と「IELTS アプリ」を
別のアイデアとして 2 つのディレクトリに分けて記録していた。同日中に 1 つへ統合したため、
以下は当時の 2 ファイルを時系列で連結したものである。過去のエントリは書き換えていない。
そのため「別アイデアとして切り出す」「ライブラリを保留する」といった、
現在は無効になった決定も残っている。末尾の統合エントリと併せて読むこと。

---

## 第 1 部: 語彙データとスキーマについての決定（旧 english-vocab-list）
## 2026-09-07 製品の軸は目的別を主、CEFR レベルを従とする

- 決定: 入口は「IELTS 用」「TOEIC 用」のような目的で切る。CEFR レベルは絞り込みや並べ替えに使う従属的な軸として持つ。目的とレベルのマッピングは、準備段階で LLM の判断に委ねる。
- 理由: 学習者の動機は目的に紐づいており、「B1 レベル」では何のために使うかが伝わらない。日本の学習者に CEFR の実感が薄いことも効いている。
- 却下した案: CEFR レベルを主軸にする — データは最も素直で A1〜C2 が約 9,900 語そのまま使えるが、なぜ使うかがユーザーに伝わらない。
- 却下した案: 軸を決めずにデータだけ統合して先に進む — 後戻りはしないが、体験の設計が始められない。

## 2026-09-07 最初に掘るのは IELTS 用とする

- 決定: 3 つの初期案（IELTS 用 / TOEIC 用 / はじめての人と仲良くなるための語彙）のうち、IELTS 用を最初の一本にする。IELTS のバンドと語彙のレベル対応は LLM に作らせる。
- 理由: 市場が大きく学習者の動機が強い。公式リストが存在しない点はどの案でも同じ条件になる。
- 却下した案: はじめての人と仲良くなるための語彙 — 既存リストが埋めていない唯一の領域で差別化の余地は大きいが、収録すべき語の定義がゼロからになる。
- 却下した案: TOEIC 用 — TSL 1,250 語という既製の答えがあり最も早く形になるが、日本で最も競合が濃く mikan や abceed と正面衝突する。
- 却下した案: 競合調査をしてから決める — 判断の精度は上がるが、この段階では時間がかかりすぎる。

## 2026-09-07 語彙リストは LLM 生成のみで完結させ、既存の公開リストは一切使わない

- 決定: 配布する語彙データは LLM の知識から生成したものだけで構成する。CEFR-J・NGSL ファミリー・Octanove などの既存公開リストは、配布物にも検証用の物差しにも使わない。根拠のなさは「あくまで目安・参考」として明示する。
- 理由: 外部データセットへの依存を避けたい。ライセンス表示義務・ShareAlike・出典管理といった運用コストが一切発生しない状態を優先する。
- 却下した案: 既存リストを配布物に同梱する — 無料かつ商用可だが、CC BY-SA の ShareAlike と CEFR-J の引用義務がついてくる。
- 却下した案: 既存リストを配布しない検証用の物差しとして手元に置く（メインセッションの推奨だった） — ShareAlike も帰属表示も配布時にしか発動しないため義務はゼロで、「TSL の何 % をカバー」といった品質指標が出せる利点があった。オーナーは依存を完全に断つことを優先して却下。
- 補足（オーナーが認識した上で受け入れたリスク）: 生成物は「なぜこの語が入っているか」を外部の基準で説明できない。生成のたびに内容が変わるため回帰テストが書けない。免責文は期待値を下げるが品質は上げない。

## 2026-09-07 IELTS アプリを先に作り、語彙ライブラリは後から切り出す

- 決定: 語彙データはまず IELTS アプリの中に持つ。必要なフィールドと粒度が固まった段階で、npm / pnpm で配布する TypeScript ライブラリとして切り出す。
- 理由: アプリが 1 本もない状態で複数アプリ向けの API とスキーマを決めるのは早すぎる抽象化になる。語彙データに何が必要かはアプリを作ってみないと分からない。
- 却下した案: ライブラリを先に作る — データ層が最初から独立するが、利用者不在でスキーマを決めることになる。

## 2026-09-07 次の一手はオーナーへのヒアリング、その後に競合・市場調査

- 決定: まず IELTS の単語学習でオーナー自身が何に困ったかを聞き取り、overview.md を起こす。その後で競合調査と市場調査を行う。競合アプリの実操作までは行わない。
- 理由: 一人称の痛みが最も早く、かつ最も強い出発点になる。調査はその裏付けに使う。
- 却下した案: 先に競合調査から入る — 根拠は強くなるが、何を探すべきかが定まらないまま調査コストを払うことになる。

## 2026-09-07 訂正: 主題は語彙リストのライブラリ。IELTS アプリ関連の 3 件は仮定への回答であり決定ではない

- 決定: このアイデアの主題は「レベル情報を持つ語彙リストを npm / pnpm のライブラリとして作る」ことである。上の 2026-09-07 のうち「最初に掘るのは IELTS 用とする」「IELTS アプリを先に作り、語彙ライブラリは後から切り出す」「次の一手はオーナーへのヒアリング、その後に競合・市場調査」の 3 件は決定として扱わない。
- 理由: オーナーの訂正（原文）「最初はライブラリとして語彙リスト(レベル感とかをもつ)だけ作ろうと思っていたが、IELTS英単語アプリを作るなら？という会話に途中で移っていったので、その答えとして回答しました」。アシスタントが「3 つの初期案のうちどれを最初に掘るか」と問うた時点でアプリ前提に会話を寄せており、オーナーはその仮定に答えていた。
- 有効なまま残る決定: 「製品の軸は目的別を主、CEFR レベルを従とする」と「語彙リストは LLM 生成のみで完結させ、既存の公開リストは一切使わない」。どちらもライブラリの設計に直接効くため維持する。
- 残るもの: IELTS 学習者としてのヒアリング結果（`overview.md` に記録）は、ライブラリが誰の何に使われるかの要件入力として有効。ただし overview.md は現在アプリを主語に書かれており、書き直しが要る。
- 注: 追記のみの原則により過去のエントリは書き換えていない。上記 3 件を読むときは必ずこのエントリと併せて読むこと。

## 2026-09-07 語の同一性のキーは「見出し語 + 品詞」とする

- 決定: 語彙データの主キーを (headword, pos) にする。同じ見出し語でも品詞が違えば別レコードとし、レベルも品詞ごとに付ける。語義（sense）では分けない。
- 理由: CEFR-J が 7,801 行の実運用でこの粒度を採っており、手元でパースして確認したところ 6,868 のユニーク見出し語のうち 848 が品詞違いで複数行に分かれていた（`research/vocab-data-schemas.md` のメインセッション確認節）。`fine` は形容詞 A1 / 名詞 B1 / 副詞 B2 / 動詞 B2 と品詞でレベルが割れるため、見出し語だけをキーにすると正しいレベルを付けられない。
- 却下した案: 見出し語だけをキーにする — 最も単純でデータ量も小さいが、`fine` を A1 と B2 のどちらに置くか決められず、レベル付与の精度が落ちる。
- 却下した案: 語義単位（WordNet 方式） — `bank` の「銀行」と「土手」を分けられて最も正確だが、生成量が数倍になる。外部の物差しを持たないと決めた以上、その量の品質を担保する手段がない。
- 承知した上での割り切り: `bank` の「銀行」と「土手」は同じ名詞レコードに畳み込まれる。CEFR-J も同じ割り切りをしている。

## 2026-09-07 目的は語のレコードに属性として持たせる

- 決定: 1 語 1 レコードを保ち、目的（IELTS / TOEIC / 日常会話など）は語のレコード上の属性として持つ。目標スコアを持つ目的については、その目標値をオブジェクトで持つ。目的ごとに独立したリストのファイルを作らない。
- 理由: 重複が構造的に起きない。将来「目標スコア」という第 3 の軸を足すときも、属性の形を変えるだけで済む（`decisions.md` 第 2 部 2026-09-07 の方向づけ）。npm で配布する静的データとして結合が不要で最も素直。
- 却下した案: NGSL 式の排他階層（コアリスト + 目的別の差分リスト） — 学習順序が自明になる利点があり、NGSL プロジェクトが実際に採用している設計だが、目的別リストが単体で完結せず常にコアとの合成が要る。
- 却下した案: 語マスタ + 所属関係に正規化 — 目的ごとの順位や重みを持てるが、公開データセットに確立した実例が見つからず、同梱 JSON としては結合が必要で重くなる。
- 材料: `research/vocab-data-schemas.md`

---

## 第 2 部: アプリの体験についての決定（旧 ielts-vocab-app）
## 2026-09-07 IELTS 英単語アプリを独立したアイデアとして切り出す

- 決定: `english-vocab-list`（語彙リストのライブラリ）の検討中に派生した IELTS 英単語アプリの構想を、独立したアイデアとして分ける。ライブラリとアプリを並行して持つ。
- 理由: オーナーの当初からの計画はライブラリ単体を作ることであり、アプリの話はアシスタントが会話を寄せた結果として出てきた。同じディレクトリに混ぜると、どちらの決定なのかが読めなくなる。
- 引き継ぎ: `english-vocab-list/decisions.md` の 2026-09-07 のエントリ群、特に訂正エントリを併せて読むこと。ヒアリング結果は本ディレクトリの `overview.md` にある。
- 却下した案: このディレクトリを作らず、アプリの話を捨てる — 最も軽いが、ヒアリングと競合調査 8 製品分の成果を失う。
- 却下した案: ライブラリのディレクトリに同居させたまま進める — 分ける手間はないが、主題が二重になる。

## 2026-09-07 直近は IELTS 特化アプリ。将来は目標設定型の統合アプリに拡張しうる（方向づけ）

- 決定: 直近に作るのは IELTS 特化の英単語アプリとする。将来、アカウント設定で目標（IELTS 7.0、TOEIC 800 など）を設定し、それに応じた語彙学習を出す 1 つの統合アプリへ拡張する道を、あり得る展開として想定しておく。
- 理由: オーナーの言葉「たとえば一つの巨大な英単語アプリというのを作り、アカウント設定として目標としてたとえばIELTS7.0、とかTOEIC800とか、そういうのを設定し、それに適切な単語学習をできるように、といった場面があるかもしれない。直近は一旦IETLS特化のアプリを作って、そこから拡張などがあり得ると思ってる」。
- 位置づけ: 統合アプリ側は「あり得る展開」であって決定ではない。IELTS 特化アプリを作ることだけが直近の方向づけ。
- 設計への影響: 目標スコアを軸にすると、CEFR レベルでも目的別リストでもない第 3 の軸（目標スコア）が要る。IELTS 7.0 や TOEIC 800 と語彙の対応には公式の根拠が存在しないため（`research/wordlists-exams.md`）、この対応表は完全に自作の主張になる。`english-vocab-list` のスキーマは、この 3 軸目を後から足せる形にしておく必要がある。

## 2026-09-07 書いた文への返しは LLM の添削と講評とする

- 決定: ユーザーが書いた文に対して、LLM が添削と講評を返す。模範解答を見せるだけで済ませない。
- 理由: 自分の文に対する返しがなければ産出練習は成立しない。既存 8 製品がここをやっていないことは競合調査で確認済み（`research/competitors/_comparison.md`）。
- 却下した案: 模範解答を見せるだけで LLM を使わない — 安くて速いが、自分の文のどこが悪いか分からない。
- 却下した案: 選んだ語がその場面で自然かだけを返す — 応答が短く安いが、文全体の質に踏み込めない。
- 残る論点: コストと応答時間、モデル選定、出力形式の安定。`open-questions.md` に追加する。

## 2026-09-07 次は IELTS アプリの体験を先に詰める

- 決定: `english-vocab-list` の収録範囲とフィールドより先に、IELTS アプリの体験を詰める。
- 理由: 体験が決まらないとライブラリが持つべきフィールドが決まらない。候補語群を出す設計なら、語に類義関係や場面タグが要ると分かる。
- 却下した案: ライブラリの収録範囲とフィールドを先に決める — データから埋められるが、使われ方が未確定のままフィールドを決めることになる。

## 2026-09-07 語彙データはアプリ内に持つ。ライブラリ化は必要が生じてから

- 決定: 当面は IELTS 専門のアプリだけを考える。語彙データはアプリの中に持つ。他アプリでこの単語データを使いたくなった時点で、はじめてライブラリとして切り出す。将来 TOEFL など他の目的に対応する余地は残す。
- 理由: オーナーの言葉「とりあえず、一旦IELTS専門(将来はTOEFLなど他目的も対応)のアプリだけ考えよう。そしてデータはもち、そこから他アプリに単語データを活用したい、となったらはじめてライブラリ化しよう」。利用者が 1 アプリしかない段階でライブラリを切り出す理由がない。
- 引き継ぐもの: `decisions.md` 第 1 部 の 4 件はアプリ内データの設計としてそのまま有効。
  - 語の同一性のキーは「見出し語 + 品詞」
  - 目的は語のレコードに属性として持たせる（将来の TOEFL 対応と目標スコア軸はこの属性で表す）
  - 語彙リストは LLM 生成のみで完結させ、既存の公開リストは一切使わない
  - 製品の軸は目的別を主、CEFR レベルを従とする
- 却下した案: 先にライブラリを作る — データとアプリが最初から分離するが、利用者不在でスキーマと API を決めることになる。
- 影響: `../english-vocab-list` は保留にする。再開の条件はそちらの `open-questions.md` に書く。

## 2026-09-07 出題は「候補語群から選ぶ → 即時に正誤 → 確定した語で自分の文を書く」

- 決定: 場面と候補語群を提示し、ユーザーが語を選んだ瞬間に正誤を返す。正誤が確定してから、その語を使って自分の文を書かせる。選択と執筆を分ける。
- 理由: オーナーの指摘「仮に正解がひとつだったとしても、その選択肢で合っているかどうか不安になりながら文を書かせるのは心理負荷が高い」。選択の不安を執筆に持ち越さないため、正誤を先に確定させる。即時フィードバックは英語学習アプリでありふれた作りであり、独自性はないが確実に効く。オーナーからは「独自性だけで考えない方がいい」という釘も刺されている。
- 却下した案: 正解を一つにせず、候補すべてを使える語にする — 心理負荷はゼロになるが、「場面に合う語を選ぶ」訓練が消える。オーナーの痛みが「語が出てこない」である以上、その部分は練習の対象から外せない。
- 却下した案: 選択をやめて語を 1 つ与えて書かせる — 最も負荷が低いが、想起の練習がすべて復習側に寄る。
- 却下した案: 場面だけ渡して自由に書かせる — オーナーが明確に辛いと述べた形。

## 2026-09-07 復習は手がかりを弱めながら間隔も開ける

- 決定: 一度書いた語を再び出すとき、手がかりを一段階弱め、かつ出題の間隔も開ける。両方を併用する。手がかりの段階は「候補語群あり → 意味のヒントだけ → 場面だけ」の 3 段階とする。
- 理由: オーナーの要望「学習曲線的なのもなんか取り入れられるといいかもね」に、手がかりの段階が直接応える。間隔反復は定着に実績がある。両方を併用すると、同じ語を何度も見せるのではなく毎回少しずつ難しくなるので、オーナーが辛いと述べた「詰め込み感」を薄められる。
- 却下した案: 手がかりを弱めるだけ — 仕組みが 1 つで軽いが、定着の間隔が制御できない。
- 却下した案: 間隔反復だけ — 実績はあるが、Anki / iKnow! と同じになり、オーナーが辛いと述べた反復そのものの体験に近づく。
- 残る論点: 2 つの仕組みを同時に入れると最初の版が重くなる。段階の上げ下げの条件と間隔の決め方は未決。

## 2026-09-07 進捗は語ごとの手がかり段階の積み上げで見せる

- 決定: 進捗を「覚えた語数」ではなく「どの手がかり段階で使えたか」の積み上げで見せる。たとえば候補ありで使えた語、ヒントだけで使えた語、場面だけで使えた語をそれぞれ数える。
- 理由: オーナーが書籍をやめた理由が「進捗の実感がわかないし、達成感もない」だったため、進捗の可視化は捨てられない。語数だけを数えると mikan と同じ「単語と意味だけ」の指標になる。手がかり段階で数えれば、使える深さが伸びていることが見える。復習の設計とも同じ軸で揃う。
- 却下した案: 書いた文を蓄積して見返せるようにする — 成果物が残る達成感は強いが、伸びているかどうかは分からない。後から足せる。
- 却下した案: ストリークやポイント — 継続には効くが、学習の中身と無関係な数字になる。

---

## 第 3 部: 統合後の決定

## 2026-09-07 アイデアを 1 つに統合する。目的別の英単語アプリで、最初の版だけ IELTS に限定する

- 決定: 「語彙リストのライブラリ」と「IELTS アプリ」を分けず、1 つのアイデアとして `english-vocab-app` にまとめる。プロダクトは IELTS 専用ではなく目的別の英単語アプリとし、**最初の版だけ、お試しとして IELTS に限定する**。語彙データはライブラリにせずアプリに同梱する。ただし**将来ライブラリとして切り出せる設計にしておく**。
- 理由: オーナーの言葉「やっぱさ、ieltsに限定しない、でも最初だけはお試しのためieltsに限定する、そして語彙のリストもライブラリではなくまずは同梱する。しかし将来的にライブラリ化も考慮した設計にする。planの中のフォルダはわけなくて済むはず」。ディレクトリを分けたことで、同じ日の決定が 2 ファイルに散り、どちらの話か読めなくなっていた。
- 無効になる過去の決定:
  - 第 2 部「IELTS 英単語アプリを独立したアイデアとして切り出す」 — 統合により無効。
  - 第 1 部の訂正エントリが「主題はライブラリ」としていた点 — 主題はアプリであり、語彙データはその一部。
  - `english-vocab-list` の保留 — アイデアとして消滅し、本ディレクトリに吸収された。
- 有効なまま残る決定: 第 1 部の「製品の軸は目的別を主、CEFR レベルを従」「語彙リストは LLM 生成のみ」「語の同一性のキーは見出し語 + 品詞」「目的は語のレコードに属性として持たせる」、第 2 部の体験に関する 3 件（出題 / 復習 / 進捗）と「書いた文への返しは LLM の添削と講評」。
- 設計上の含意: 語彙データは、アプリのロジックから切り離せる形でアプリ内に置く。目的（IELTS / TOEFL / TOEIC など）は語のレコード上の属性なので、最初の版で IELTS だけを収録しても、後から他の目的を足すのにスキーマを変える必要がない。切り出しが必要になったら、データ部分をそのままパッケージにできる。
- 却下した案: ディレクトリを分けたまま進める — 分ける手間はないが、同じ日の決定が 2 ファイルに散る。
- 却下した案: 最初から複数目的を収録する — 拡張性は示せるが、お試しの版が重くなる。

---

## 第 4 部: シンプルな単語カードアプリへの方針転換（2026-09-10）

2026-09-10 のセッションで、オーナーが方針を大きく絞った。手本は、オーナーが
`tomada-routine` リポジトリで運用している Anki の Vocabulary デッキ
（`.claude/skills/anki-vocabulary/`、2026-09-10 時点で 110 枚）。

## 2026-09-10 オーナー自身がローカルで動かす自前アプリにする。デプロイは後で考える

- 決定: 最初の版は、オーナーが自分のローカル環境で動かすアプリとする。公開・デプロイは後で考える。学習画面は Anki に任せず、自前で作る。
- 理由: オーナーの言葉「一旦さ、もうシンプルに、私がローカルで動かすアプリとします。デプロイは後に考える」「自前アプリ前提でいい」。
- 却下した案: 生成したカードを Anki のデッキとして書き出すだけにする（メインセッションの推奨だった） — 間隔反復・統計・同期を Anki がすでに持っているため、作業をカードデータに集中できる。オーナーは自前アプリを選んだ。Anki に対して自前アプリが何を足すかは `open-questions.md` に置く。
- 却下した案: Anki への書き出しと自前アプリの両方を作る — 柔軟だが、最初の版の作業が一番多い。

## 2026-09-10 アプリに AI を組み込まない。AI は事前のデータ準備にだけ使う

- 決定: 実行時には LLM を呼ばない。AI を使うのは、同梱するカードデータを事前に生成・点検する工程だけとする。
- 理由: オーナーの言葉「事前データ準備にはAIを使うけど、アプリ自体にはAIは組み込まない、シンプルな形としたい」。
- 却下した案: 書いた文を LLM が添削・講評する（第 2 部 2026-09-07 の決定） — 既存 8 製品に対する唯一の差分だったが、実行時のコスト・応答時間・モデル選定・出力の安定という未決の論点をまとめて抱える。
- 無効になる過去の決定:
  - 第 2 部「書いた文への返しは LLM の添削と講評とする」。
  - 第 2 部「出題は候補語群から選ぶ → 即時に正誤 → 確定した語で自分の文を書く」。カード形式（下記）に置き換わる。
  - 第 2 部「復習は手がかりを弱めながら間隔も開ける」のうち手がかり 3 段階。表面は常に同じ構成になる（下記）。間隔を開ける部分は下の「自己採点・Anki と同じ考え方」で残る。
  - 第 2 部「進捗は語ごとの手がかり段階の積み上げで見せる」。段階がなくなるため、数える軸がなくなる。進捗の見せ方は `open-questions.md` に戻す。
- 承知した上での割り切り: 自分の文を書く産出練習は、このアプリからなくなる。既存 8 製品に対する差分もなくなる。個人用の道具として作るので、差分は問わない。

## 2026-09-10 語のレベル付けは自称でよい

- 決定: 語ごとのレベル（CEFR・IELTS バンドなど）は自称でよい。外部の根拠は求めない。
- 理由: オーナーの言葉「ワードごとの振り分けレベルみたいなのは自称で構わない。どうせ使う側も、私も気にしないから」。
- 影響: `open-questions.md` の「IELTS のバンドと語彙の対応をどう作り、どう名乗るか」は、この決定で解決する。

## 2026-09-10 カードの形: 表は英語の意味説明と穴埋め例文を常に両方、裏は語と短い例文

- 決定: 表面に英語の意味説明と、答えを `___` で伏せた穴埋め例文を常に両方載せる。裏面に語と、その語を使った短い例文を載せる。将来は語だけでなく熟語やフレーズも扱う。
- 理由: オーナーの言葉「英語で意味を説明し、裏表のカード形式」「穴埋め式での例文も表にあったらいいな。文脈で結局覚えないといけない」。意味説明だけでは類義語（improve / enhance / boost）で答えが 1 つに決まらない。穴埋め文脈が答えを絞る。
- 却下した案: 表は意味説明だけで、穴埋め文はヒントとして後から開く — 思い出す負荷は高いが、意味説明だけの段階で類義語に答えが割れる。
- 却下した案: 穴埋め文を主にし、意味説明は補助にする — 文脈重視には最も合うが、意味説明の出番が薄れる。
- 影響: `open-questions.md` の「場面に合う語は 1 つに定まるか」は、「表面から答えが一意に決まるか」という形でカード単位の品質の問題に変わる。

## 2026-09-10 答え合わせはめくって自己採点。復習の間隔は Anki と同じ考え方にする

- 決定: 表面を見て頭の中で答え、裏をめくって記憶の度合いを自分で選ぶ。その選択に応じて次の出題間隔を決める。ロジックは Anki と同じでよい。
- 理由: オーナーの言葉「自分で記憶度合いを選んで復習に役立てる。一旦、Ankiと同じロジックでいいぐらい」。実行時に AI を使わない前提で最も単純で、穴埋めに入る語形の揺れ（improve / improved）の問題も起きない。
- 却下した案: 入力して完全一致で判定する — つづりまで思い出す練習になるが、変化形を許すリストを持つか、例文の空欄を原形に限る制約が要る。
- 却下した案: 入力してから自己採点 — 語形の問題は起きないが、操作が一番多い。
- 残る論点: Anki の SM-2 と FSRS のどちらに合わせるか。

## 2026-09-10 意味説明の語彙レベル（A1〜A2、上限 B1）は生成時の目安にとどめ、機械では確かめない

- 決定: 意味説明を A1〜A2（上限 B1）の英語で書くのは、AI に生成させるときの目安とする。判定用の語彙リストは持たない。
- 理由: オーナーの言葉「意味説明のそれはあくまで目安。AIに生成させるときの目安です」。
- 却下した案: 説明に使ってよい語のリストも LLM で生成して固定し、Lint で照合する（メインセッションの推奨だった） — 判定が毎回同じになるが、目安で足りるなら過剰。
- 却下した案: CEFR-J を判定用に手元に置く — 第 1 部で却下済みの案を戻すことになる。

## 2026-09-10 カードデータは生成用の Skill を回して足りない範囲を埋め、生成後に点検・修正用の Skill を通す

- 決定: カードの生成は Skill として用意し、足りないと思う範囲を回すたびに埋めていく。一度に全量を生成しない。生成後に点検し、問題があれば修正させる Skill も別に用意する。
- 理由: オーナーの言葉「AIに生成させるときはSkillを用意し、足りないと思われる範囲をどんどん回すたびに埋めていく感じ。生成後にチェックして随時修正させるSkillsも必要やね」。
- 影響: `open-questions.md` の「最初の版に何語収録するか」は、最初に回す範囲をどう決めるかという問いに変わる。

## 2026-09-10 カードの項目は、表 = 英語の意味説明 + 穴埋め例文、裏 = 語 + 例文 2 本。日本語は持たない

- 決定: 表面に英語の意味説明と穴埋め例文を載せる。裏面に語と、その語を使った例文を 2 本載せる。日本語訳はカードに持たせない。
- 理由: オーナーの選択。手本の Vocabulary デッキから日本語訳だけを外した形。例文を 2 本にするのは、穴埋め文とは別の場面での使い方を 2 通り見せるため（Vocabulary デッキの P2・P8 と同じ考え方）。
- 却下した案: Vocabulary デッキと同じく、裏面に短い日本語訳も持つ（メインセッションの推奨だった） — 意味の取り違えに答え合わせの最後で気づける。オーナーは日本語を持たない形を選んだ。
- 却下した案: 例文 1 本・日本語なし — 最初に述べた形で最も軽いが、別の場面での使い方が 1 通りしか見えない。
- 承知した上での割り切り: 意味を取り違えて覚えていても、カードの中にはそれを確かめる手段がない。

## 2026-09-10 Lint は語数・文字数に加え、判断の要らない形のチェックまで含める

- 決定: Lint で次を弾く。各項目の語数・文字数の上限、答えの漏れ（表面に答えの語やその変化形が出ている）、裏面の例文が答えの語を使っていない、例文が穴埋め文の空欄を埋めただけの焼き直し、穴埋め文に空欄マーカーがない。答えが一意に決まるか・例文が自然かといった判断の要る点は、点検用の Skill に任せる。
- 理由: どれも文字列の照合で判定でき、`tomada-routine` の `.claude/skills/anki-vocabulary/scripts/validate_phrase_cards.py` に実装がある。同リポジトリの `English/payloads/2026/09/phrase-jobposting-20260907-001.json` では、AI が生成した 6 枚のうち 5 枚で、裏面の例文に答えの語が一度も出ていなかった（`oversee` の例文が "She watches over …" など）。
- 却下した案: 語数と文字数だけ（オーナーが最初に述べた形） — Lint は最小になるが、機械で確実に止められる誤りを LLM の点検に頼ることになり、見落としがありうる。

## 2026-09-10 復習間隔のロジックは FSRS に合わせる

- 決定: Anki の新しいスケジューラである FSRS と同じ考え方で、復習の間隔を決める。評価は Again / Hard / Good / Easy の 4 段階の自己申告とする。
- 理由: 同じ定着率を少ない復習回数で出せる。公開ライブラリを使えば、自分で書く量は SM-2 より少ない。事実関係は `research/spaced-repetition-fsrs.md` で確認する。
- 却下した案: SM-2（従来の Anki） — 数十行で書け、挙動を説明しやすいが、間隔の伸び方が単純で、難しい語が何度も出続けやすい。
- 却下した案: 独自の簡易版（決まった日数で進める） — 最も軽いが、「Anki と同じ」から離れる。

## 2026-09-10 自前アプリで Anki に足すもの: 見た目と操作、目的・トピック別の絞り込み、進捗と達成感

- 決定: 自前アプリにする理由、すなわち最初の版で必ず作る部分は次の 3 つ。Anki の見た目と操作を自分好みにすること、目的・トピック別に出題範囲を絞り込めること、進捗と達成感を自分好みに見せること。
- 理由: オーナーの言葉「あくまでIELTS、TOEFLなど、いろんな目的別に使えるようにしたいってわけ。なお、将来的には海外生活で病院いくとき〜とかそういうトピックで学ぶとかもしたい。とにかくボキャブラリーを増やしたいのよ」。
- 却下した案: 生成から学習までの一本化（Anki への投入や note_id 管理の手間をなくす）を理由に含める — オーナーは選ばなかった。
- 位置づけ: 目的（IELTS / TOEFL など）に加えて、生活場面のトピック（海外生活で病院に行く、など）も将来の学習単位になる。第 1 部の「目的は語のレコードに属性として持たせる」は維持し、トピックの持ち方は `open-questions.md` に置く。最初の版を IELTS に限る決定（第 3 部）は変えていない。
- 注: 狙いの重心は「覚えた語を自分の文で使える」から「語彙を増やす」へ移っている。ただしカードは意味から語を思い出す向きなので、語を引き出す練習であることは変わらない。

## 2026-09-10 目的は語の属性として複数持てる。トピックは固定の一覧から選ぶ複数タグにする

- 決定: 1 語に目的（IELTS / TOEFL など）を複数付けられる。トピック（environment、health、hospital など）も複数付けられるタグとし、名前は固定の一覧から選ぶ。
- 理由: オーナーの言葉「目的は属性、トピックは複数タグやね。でも目的は複数もあるだろう」。`symptom` のような語は IELTS の Health にも、海外生活の病院にも属する。同じ語のカードを二重に持たずに済む。第 1 部「目的は語のレコードに属性として持たせる」とも整合する。
- 却下した案: 目的 > トピックの階層で 1 語 1 所属 — 最も分かりやすいが、複数にまたがる語を二重に持つか、どちらかに寄せることになる。
- 却下した案: 自由タグ — 柔軟だが、表記が揺れて絞り込みと集計が崩れる。
- 残る論点: 目的ごとのレベル（目標スコア）を語にどう持たせるか。`open-questions.md` に置く。

## 2026-09-10 生成 Skill は IELTS のトピック単位と目標スコア単位の両方で回す（方向づけ）

- 決定: 生成 Skill を回す単位として、IELTS のトピック（Environment など）で切る回し方と、目標スコア（IELTS 7.0 向けなど）で切る回し方の両方を持つ。
- 理由: オーナーの言葉「IELTSのトピックで切るし、目標スコア基準でも回したいよねえ」。
- 位置づけ: 方向づけ。目標スコアで切る回し方は、レベルの持ち方（未決）が決まるまで具体化できない。最初に回す量も未決。

## 2026-09-10 進捗と達成感の見せ方は、別のセッションで決める

- 決定: 進捗と達成感の作り込みは、このセッションでは決めない。別のセッションで扱う。このセッションの最後に、そのセッションに渡す依頼プロンプトを `/refining-prompts` で作る。
- 理由: オーナーの言葉「進捗と達成感といった作り込みは、別セッションで決める。最後に /refining-promptsで依頼プロンプト見せて。ここは賢いモデルの力が必要だ」。
- 却下した案: なし。このセッションで出した 3 案（トピック別の定着状態を軸にする〔メインセッションの推奨〕、学習量の記録、両方）は却下ではなく、別のセッションへの持ち越し材料とする。

## 2026-09-10 アプリは nextjs-app-template から切り出して作る

- 決定: アプリは `~/ghq/github.com/tomada1114/nextjs-app-template` を元に作る。ローカルで localhost に立ててブラウザで使う。
- 理由: オーナーの言葉「~/ghq/github.com/tomada1114/nextjs-app-templateから切る予定」。テンプレートは Next.js 16（App Router）・TypeScript・pnpm・next-intl・zod という構成。LLM 呼び出しの層は丸ごと外せる設計になっており、「アプリに AI を組み込まない」決定と合う（2026-09-10 時点の同リポジトリ README で確認）。
- 却下した案: デスクトップアプリ（Tauri など） — アプリらしく起動できるが、ビルドと配布の手間が増え、後で Web に出すときに作り直しに近くなる。
- 却下した案: スマホで使う — 隙間時間に使えるが、「ローカルで動かす」と衝突する。
- 残る論点: テンプレートにデータベースはない。カードデータ（生成物）と復習履歴をどこに置くかは `open-questions.md` に置く。

## 2026-09-10 カードデータは JSON ファイルで git 管理し、復習履歴はローカルの SQLite に置く

- 決定: 生成したカードは JSON ファイルとしてリポジトリで git 管理し、Lint もファイルに対して回す。復習履歴と FSRS の状態は、ローカルの SQLite に置く。
- 理由: 生成・修正の差分が git で読める。履歴は SQLite なら進捗の集計が書きやすく、ファイル 1 つでバックアップできる。デプロイするときは、履歴の置き場だけを差し替えればよい。
- 却下した案: 履歴をブラウザ内（IndexedDB）に置く — サーバー側に保存の仕組みが要らないが、ブラウザのデータを消すと履歴も消え、バックアップしにくい。
- 却下した案: 履歴も JSON ファイルにする — 最も単純だが、履歴が増えると重くなり、書き込みの途中で落ちると壊れやすい。

## 2026-09-10 生成 Skill の最初の 1 回は、IELTS の 2〜3 トピック × 各 30 語前後とする

- 決定: 最初に生成 Skill を回す範囲は、IELTS のトピック 2〜3 個、各 30 語前後（合計 60〜90 枚）とする。
- 理由: トピックでの絞り込みが意味を持ち、生成 → Lint → 点検の流れとカードの質を、全部自分で目を通して確かめられる量。
- 却下した案: 1 トピック 20 語で試作 — カードの質は最速で確かめられるが、絞り込みの手応えは試せない。
- 却下した案: 決めずに、プロダクト側のリポジトリで決める — 先送りする理由がない。
- 残る論点: どのトピックを選ぶか。

## 2026-09-10 語のレベルは CEFR 6 段階で 1 つだけ持ち、目標スコアは試験ごとの換算表で CEFR に変換する。出題は目標の段階と 1 つ下

- 決定: 1 語に CEFR のレベル（A1〜C2）を 1 つだけ持たせる。目標スコア（IELTS 7.0 など）は、試験ごとに 1 つ持つ換算表で CEFR に変換する。目標を選んだときは、目的が一致し、レベルが目標の段階かその 1 つ下の語を出す（IELTS 7.0 → C1 なら B2〜C1）。生成 Skill を「目標スコアで回す」ときも、同じ換算で「その目的のトピックで、目標の CEFR の語」と読み替える。
- 理由: 精度は要らないとオーナーが明言している（「正直、そんな正確じゃないてもよいかと」）。そうなると効くのは、生成のたびに同じ語の値が揺れないことになる。LLM にとって、語ごとの CEFR レベルは広く流通した概念である。一方、語と試験スコアの対応は試験の実施団体を含めて誰も作っておらず、特に 0.5 刻みの値は語の単位ではノイズになる。目標の 1 つ下まで出すのは、目標より下の段階にも抜けがあるため。換算の粗さ（例: IELTS 6.0 と 6.5 が同じ段階になる）は、オーナーが許容済み（「対応表がCEFRとかは荒いのは問題ないっす」）。
- 却下した案: 試験ごとにスコアを持つ（ielts: 7.0 など） — 生成も画面も直接的で、試験を足すたびに全語へ値を追加する手間はオーナーが許容していた（「試験の数には限りもあるから、全部に対応表を載せたり、再生成（というか追加）するのはしゃーない」）。しかし語ごとの点数は生成のたびに揺れやすい。
- 却下した案: CEFR と試験スコアの両方を持つ — 最も柔軟だが、値が食い違ったときにどちらを信じるかが決まらない。
- 却下した案: CEFR に中間段階（B1+ など）を足す — 少し細かく分けられるが、粗さが問題にならない以上は過剰で、生成時の揺れも増える。
- 却下した案: 出題は目標の段階だけ — 狙いは最も明確だが、1 つ下の段階の抜けに気づけない。
- 却下した案: 出題は目標までの全段階 — 漏れはないが、簡単な語が大半を占めて詰め込み感が出る。
- 前提の確認: 試験の実施団体がスコアと CEFR の対応表を公表しているかは、`research/exam-cefr-mapping.md` で確認中。公表されていなければ、換算表も自称で作る（レベルは自称でよいという 2026-09-10 の決定の範囲内）。
- 影響: `open-questions.md` の「IELTS のバンドと語彙の対応をどう作り、どう名乗るか」は解決する。第 1 部の「製品の軸は目的別を主、CEFR レベルを従とする」とも整合する。

---

## 第 5 部: 進捗と達成感の見せ方（2026-09-11）

第 4 部 2026-09-10「進捗と達成感の見せ方は、別のセッションで決める」を引き継いだ
セッション。メインセッション（Fable）が選択肢とトレードオフを提示し、オーナーが
AskUserQuestion で 7 点を明示的に決めた。以下はすべて、メインセッションの推奨を
オーナーがそのまま選んだもの。

## 2026-09-11 進捗の主指標は「今覚えている語数」。未学習の残り枚数を従として添える

- 決定: 進捗の主指標を「今覚えている語数」とする。定義は、一度以上復習したカードの
  想起確率 R の合計（Anki の「推定総知識量」と同じ定義）。整数に丸めて表示する。
  未復習のカードは 0 として扱う。未学習の残り枚数を従として添える。
- 理由: 初日から動く。復習をサボると正直に減る。学習の中身（文脈から語を引き出せる
  確率）そのものを表す。Anki の統計画面もデッキや検索で絞った「推定総知識量」を
  出せるが、学習画面の範囲選択とは別の画面であり、目的・トピック・目標レベルという
  このアプリの軸に揃えて学習の流れの中に置くことが差分になる（出典:
  https://docs.ankiweb.net/stats.html「arbitrary searches」の記述、2026-09-11 確認）。
  未学習の残りを添えるのは、60〜90 枚は数週間で
  覚え尽くして指標が頭打ちになるため、頭打ちを「停滞」ではなく「次のトピックを
  生成 Skill で足す合図」として見せるため。
- 材料: 想起確率 R は ts-fsrs の `get_retrievability(card, date)` で任意の日付について
  計算できる（ts-fsrs リポジトリ `_autodocs/01-scheduler-api.md`、Context7 経由で
  2026-09-11 に確認）。Anki は FSRS モードで「Estimated total knowledge（推定総知識量）」
  を出しており、定義は「復習済みカードの平均想起確率 × 復習済みカード数」
  （出典: https://docs.ankiweb.net/stats.html、2026-09-11 にメインセッションが確認）。
- 却下した案: 「未学習 / 学習中 / 定着（安定度が一定以上）の 3 区分」（2026-09-10 の
  メインセッション推奨） — 分かりやすいが、定着に届くのは数週間後で、離脱が起きる
  最初の数日に一番動かない指標になる。定着の区分は最初の版では出さず、使ってみてから
  足すか判断する。
- 却下した案: 「3 区分と合計の両方」 — 数字が 4 つ並び、どれを見れば伸びたと
  分かるのかがぼやける。
- 過去の却下の見直し: 2026-09-07「進捗は語ごとの手がかり段階の積み上げで見せる」で
  却下した「覚えた語数」は、当時の却下理由が「mikan と同じ単語と意味だけの指標」
  だったが、今のカードは意味説明と穴埋め文脈から語を引き出す形なので数える対象
  そのものが変わり、理由は成り立たなくなった。ただし「覚えた / まだ」の二値ではなく、
  想起確率という連続値で数える形で復活させる。

## 2026-09-11 伸びは、日次の推移カーブとセッション終了時の差分で見せる

- 決定: 「今覚えている語数」を日ごとに折れ線で見せる。セッション終了画面に
  「今日 M 枚復習、覚えている語が +N」とカーブ上の今日の点を出す。
- 理由: 「増えている」実感は時間軸との比較でしか得られない。書籍の単語帳になかった
  のはこれ。作るものは 1 つの値の日次記録と 1 本のグラフで小さい。
- 却下した案: 「現在値 + セッション差分のみ、カーブは後回し」 — 軽いが 1 週間前との
  比較ができない。
- 却下した案: 「現在値だけ」 — 開くたびに数字が 1 つあるだけで、書籍の単語帳と
  大差ない。
- 残る論点: 日次の値を、日ごとにスナップショットとして SQLite に記録するか、
  ReviewLog に残る復習前の stability から過去の任意日の R を再計算して出すかは
  どちらも可能で（ts-fsrs の `ReviewLog` は rating・review 日時に加え、復習前の
  stability / difficulty / state / due / scheduled_days を持つ。出典: ts-fsrs
  リポジトリ `_autodocs/02-types.md`、Context7 経由で 2026-09-11 に確認）、
  プロダクト側で決める。`open-questions.md` に置く。

## 2026-09-11 毎日開くきっかけは、今日の期限カード数だけ。ストリークやポイントは持たない

- 決定: 毎日開くきっかけは、FSRS が計算する今日の期限カード数のみとする。連続日数
  やポイントは持たない。
- 理由: FSRS が計算する due をそのまま出すだけで追加設計が要らず、学習の中身と
  直結する。復習を空けるとカーブが下がるので、それ自体が戻る理由になる。
- 却下した案: 「期限カード数 + 連続日数」 — 続けるには効くが、2026-09-07 の却下理由
  「学習の中身と無関係な数字」が今も成り立つ。途切れた日に逆に離脱を招きやすい。
- 却下した案: 「忘れかけの語数（想起確率が 90% を切った語）」 — 損失回避の動機は
  効くが、開くたびに減点を突きつけられる形になり、詰め込み感が辛かったオーナーには
  逆効果の恐れ。
- 位置づけ: 「続ける仕組み」と「伸びの実感」は別物と整理した。最初の版では伸びの
  実感を優先し、続ける仕組みは FSRS がただで出す期限数に留める。

## 2026-09-11 目標スコアまでの距離は、目標範囲の語群に対する「覚えている割合」だけで見せる。スコア推定はしない

- 決定: 例えば IELTS 7.0 → C1 なら、目的 = IELTS かつレベル = B2〜C1 の語群について
  「今覚えている語数 / 語群の総数」を割合で出す。「7.0 まであと何点」とは言わない。
- 理由: 語のレベルは自称（2026-09-10 決定）、換算は粗く（許容済み）、語群は自分で
  生成した 60〜90 枚。この材料でスコアを推定しても根拠がなく、信じられない数字は
  達成感にならない。割合なら正直で、換算の粗さも問題にならない。
- 却下した案: 「推定スコアを出す（今の推定 6.8 など）」 — abceed や WordSprint に
  近く分かりやすいが、根拠がない。
- 却下した案: 「距離は出さず、目標は出題範囲の絞り込みにだけ使う」 — 最も軽いが、
  目標に近づいている感覚が一切得られない。

## 2026-09-11 1 回のセッションは、期限カードを全部こなし、新規カードを上限 N 枚まで引いたら終わる

- 決定: Anki と同じ考え方で、期限カードを全部こなし、新規カードを上限 N 枚まで
  引いたらセッション終了とする。N は設定値で、初期値は 10 枚程度を想定する
  （値そのものは未決）。途中で閉じても履歴は残る。
- 理由: 終わりが毎日自動で決まり、「今日の分は終わった」が区切りになる。セッション
  終了時の差分を出すために「終わり」の定義が必要だった。`open-questions.md`
  「1 回の利用時間。いつ開いていつやめるか」がこれで解決する。
- 却下した案: 「固定枚数（20 枚など）で区切る」 — 所要時間は読めるが、期限カードが
  枚数を超えた日に復習が溢れ、FSRS の前提が崩れる。
- 却下した案: 「自分で『終わる』を押すまで」 — 区切りがなく、どこまでやるかを毎回
  自分で決めることになる。離脱を繰り返した履歴と相性が悪い。
- 残る論点: N の初期値。`open-questions.md` に置く。

## 2026-09-11 カーブと「今覚えている語数」は、選んでいる範囲（目的・トピック・目標レベル）に追従させる

- 決定: 「今覚えている語数」と日次カーブは、選んでいる範囲（目的・トピック・目標
  レベル）に追従して出す。範囲を外せば全体の値になる。
- 理由: 範囲を絞り込めることが自前アプリの理由の 1 つなので、進捗も同じ範囲で
  見せる。想起確率はカード単位なので、どの範囲でも同じ計算で出せる。
- 却下した案: 「全体の 1 本だけ」 — 最も単純だが、進めているトピックが伸びたかは
  分からない。
- 却下した案: 「全体 + トピック別を常に全部並べる」 — 一覧性は高いがトピックが
  増えるとグラフが並び、最初の版には過剰。

## 2026-09-11 達成感の瞬間は、セッション終了画面の差分だけに置く。節目の演出は最初の版では作らない

- 決定: 達成感の瞬間は、セッション終了画面の差分表示だけに置く。語数の節目や
  トピック完了といった節目の演出は最初の版では作らない。
- 理由: 差分は毎回必ず起き、学習の中身そのもの。節目の設計は使ってみてから足す。
- 却下した案: 「差分 + トピックの未学習が 0 になった節目」 — 「一周した、次を
  生成する」区切りになり安いが、一周と定着は別なので「終わった感」が先に来て
  復習を軽く見る恐れ。
- 却下した案: 「差分 + 語数の節目（50 / 100 語など）」 — 値は増減するので同じ節目を
  何度も越え、節目の意味が薄れる。

## 2026-09-11 最初の版で作る範囲と捨てるもの（上記 7 点の帰結）

- 決定: 新しい決定ではなく、上記 7 点の帰結を 1 箇所にまとめたもの。作る: 期限
  カード数、選択範囲の今覚えている語数と未学習の残り、日次カーブ、セッション
  終了画面の差分、目標範囲の覚えている割合。捨てる: 定着区分、ストリーク・
  ポイント、スコア推定、節目の演出、トピック別グラフの一覧、忘れかけ語の一覧。
- 影響: `experience.md`「最初の版で成立させる体験」・`overview.md`「最初の版で
  捨てるもの」をこの帰結に合わせて更新する。

---

## 第 6 部: カードデータの準備と、実装への仕分け（2026-09-11）

第 5 部と同日の別セッション。残っていた未決（トピックの固定一覧、生成 Skill と
点検 Skill の設計、Lint の閾値、小さい未決）を扱った。メインセッション（Opus）が
選択肢とトレードオフを提示し、オーナーが AskUserQuestion で明示的に決めた。
以下はすべて、メインセッションの推奨をオーナーがそのまま選んだもの。

## 2026-09-11 トピックの固定一覧は次の 15 個で確定する。追加は一覧ファイルを明示的に編集して行う

- 決定: トピックの固定一覧を education / work / business-economy /
  science-technology / environment / health / media-advertising /
  travel-tourism / cities-housing / crime-law / government-society /
  family-relationships / culture-arts / food / sport-leisure の 15 個
  （英語の kebab-case）とする。1 語に複数付けられる（第 4 部 2026-09-10）。
  一覧を増やすときは、一覧ファイルを明示的に編集する。
- 理由: IELTS は公式のトピック一覧を公表していない（IDP India は頻出
  トピックを「practice topic areas, not guaranteed test questions」と明記。
  `research/ielts-topics.md`）。一覧は自作するしかなく、語のレベルと同じく
  自称でよい（第 4 部 2026-09-10「語のレベル付けは自称でよい」と同じ扱い）。
  表記揺れを防ぐという固定一覧の目的を最初から満たし、生成 Skill がタグを
  選ぶ候補も決まる。15 個なら全部に目を通して判断できる。
- 却下した案: 使う分だけ一覧に入れて育てる — 最小で済むが、1 語に複数タグを
  付けるときの候補が 2〜3 個しかない。一覧が増えたとき、既存の語に遡って
  タグを付け直すことになる。
- 却下した案: 生活場面（hospital・banking・renting など）も含めた 25 個前後を
  今作る — 将来の目的まで網羅できるが、最初の版は IELTS に限る（第 3 部）ので、
  半分は使わないタグになる。
- 位置づけ: 海外生活の場面（病院など）は、その目的を足すときに一覧を編集して
  加える。

## 2026-09-11 生成 Skill の最初の 1 回は、environment / education / health を各 30 語前後、IELTS の B2〜C1 で回す

- 決定: 第 4 部 2026-09-10「生成 Skill の最初の 1 回は、IELTS の 2〜3 トピック ×
  各 30 語前後とする」に残っていた論点（どのトピックにするか）を閉じる。
  最初の 1 回は environment / education / health の 3 トピック、各 30 語前後
  （合計 90 枚前後）とする。語のレベルは、目標を IELTS 7.0 → C1 とみなし、
  出題範囲の決定（目標の段階とその 1 つ下）に合わせて B2〜C1 にする。
- 理由: 3 つとも IDP の練習トピック一覧に並ぶ IELTS の定番である。health は
  将来の海外生活（病院）と語が重なるので、複数タグ・複数目的の設計を最初の
  1 回で試せる。
- 却下した案: environment + education + science-technology — IELTS Writing
  Task 2 の対策としては一番まっすぐだが、抽象語に偏り、生活場面との重なりを
  試せない。
- 却下した案: environment + health の 2 つ（60 枚） — 点検の負担は一番軽いが、
  トピックの絞り込みが 2 択になり、手応えが薄い。
- 注: 目標 IELTS 7.0 は、この問いの前提としてメインセッションが置き、オーナーが
  異を唱えずに選んだもの。目標スコアそのものを独立した決定として問うては
  いない。

## 2026-09-11 生成 Skill はトピック単位で回し、（見出し語, 品詞）で重複を排除する。既存の語が新しいトピックにも合うときは、タグの追記にする

- 決定: 生成 Skill の 1 回の入力は（目的, トピック, CEFR の範囲, 語数）とする。
  生成の前に、既存カードの（見出し語, 品詞）の一覧を Skill に渡して除外させる。
  Lint でもキーの重複を ERROR にする。既存の語が新しいトピックにも合う場合は
  新しいカードを作らず、既存カードへのタグ追記として提案する。
- 理由: symptom のように複数のトピックにまたがる語が、どのトピックで絞っても
  出てくるようになる。キーの重複を機械で止めるので、生成 Skill の見落としに
  頼らずに済む。同一性のキーは第 1 部 2026-09-07「語の同一性のキーは
  『見出し語 + 品詞』とする」をそのまま使う。
- 却下した案: キーの重複を弾くだけで、タグは追記しない — 一番単純だが、後から
  回したトピックでは既存の語がそのトピックのタグを持たず、絞り込むと抜ける。
- 却下した案: 目標スコア単位（トピックを指定せず「IELTS の B2〜C1」）でも最初から
  回す — 語の幅は広がるが、トピックのタグ付けを生成 Skill の判断に丸ごと任せる
  ことになり、最初の 1 回には過剰。第 4 部 2026-09-10「生成 Skill は IELTS の
  トピック単位と目標スコア単位の両方で回す（方向づけ）」は撤回しない。
  目標スコア単位の回し方は、必要になった時点で足す。

## 2026-09-11 一意性は、表面だけを見せる「ブラインド解答」で確かめる。点検 Skill は独立した審査 2 本で構成する

- 決定: 点検 Skill は、カードを書いた実行とは別の、独立した審査として回す。
  審査は 2 本立てにする。
  - 1 本目（ブラインド解答）: 表面（意味説明 + 穴埋め例文）だけを見せ、当てはまる
    語をすべて挙げさせる。答え以外にも妥当な候補が出たら FIX とし、意味説明の
    注記や穴埋め文のコロケーションで候補を絞る案を添えさせる。
  - 2 本目（観点レビュー）: `tomada-routine` の `short-form-review.md`
    「anki-vocabulary」節と同じ観点で見る。言い換えによる答えの漏れ、例文の質、
    言い換えで骨格を隠した焼き直し、2 本の例文どうしの重複、同義語の重複の 5 つ。
  - 判定は FIX / DROP に確信度を添えて、違反のあったカードだけを返させる。
    DROP は勧告であり、最終判断はメイン側が持つ。審査の判定を覆したときは、
    理由を付けてオーナーに報告する。審査は枚数にかかわらず毎回回す。
- 理由: 「一意か？」と判断を求めると、審査がゴム印になりやすい。「表面から
  当てられるか」という観察に変えれば、候補が複数出たという事実で判定できる。
  書いた本人の点検が効かないことは、参照実装で実測されている（書いた側が自分で
  当てていた頃、定義済みの項目に 196 枚中 25 枚が違反したまま通った。
  `tomada-routine` の `.claude/skills/anki-router/references/review-doctrine.md`、
  2026-09-11 にメインセッションが直接読んで確認）。
- 却下した案: 参照実装どおり、1 回の独立レビューの観点の 1 つとして一意性を見る
  — `tomada-routine` で実績があり安いが、審査に「一意か」の判断を求める形で、
  見逃しの割合は分からない。
- 却下した案: 生成時に答えの類義語を隠しフィールドに列挙させ、表面がそれらを
  排除できているかを点検する — 構造的だが、類義語を挙げるのが書いた本人なので
  自己レビューに近く、画面に出さない項目がスキーマに増える。
- 残る論点: ブラインド解答で、どこまでを「妥当な候補」と数えるか（語形の違い、
  レジスターの違い）の線引きは、最初の 1 回の結果を見てプロダクト側で調整する。

## 2026-09-11 既存カードの修正は、（見出し語, 品詞）から決まる id を固定したまま上書きする。履歴は git に任せる

- 決定: カードの id は（見出し語, 品詞）から決める。点検 Skill や再生成で表面・
  例文を直すときは、同じ id のカードを上書きし、FSRS の状態はそのまま引き継ぐ。
  変更の前後は git diff で読む。答えの語そのものが変わる修正はキーが変わるので
  新しいカードとし、古いカードは退役扱いにする（復習履歴は SQLite に残す）。
- 理由: 生成・修正の差分を git で読めるようにした第 4 部 2026-09-10「カードデータは
  JSON ファイルで git 管理し、復習履歴はローカルの SQLite に置く」と同じ理屈で、
  版の管理を git に一本化できる。表面を直すたびに学習がリセットされない。
- 却下した案: カードに版（v1 / v2）を持たせる — どの版で覚えたかまで追えるが、
  git と二重管理になり、個人用には過剰。
- 却下した案: 学習済みのカードは直さず、未学習のカードだけを上書きする — FSRS の
  状態との整合は一番安全だが、学習中に見つかった悪いカードが残り続ける。

## 2026-09-11 Lint の閾値は tomada-routine の値を引き継ぐ。閾値は 1 か所に置き、運用しながら変える

- 決定: Lint の初期値は次の通り。意味説明 + 穴埋め例文の合計は 40 語以下、
  穴埋め例文は 20 語以下、答えの語は 5 語以下、例文はそれぞれ 12 語以下とし、
  超えたら ERROR にする。例文が 8 語を超えたら WARN（ブロックしない）を出す。
  あわせて、参照実装にある形のチェックも持ち込む。`...` と `…` の禁止、
  どの項目にも日本語（CJK）を含まないこと、穴埋め文の `___` の有無の 3 つ。
  閾値は設定の 1 か所に置き、運用しながら変える。
- 理由: 110 枚の運用で調整されてきた値である（例文の上限は 2026-09-07 に
  20 語から 12 語へ下げた経緯がある）。日本語を持たないカード（第 4 部 2026-09-10）
  なので、日本語が混入していればそれ自体が誤りになる。
- 出典: `tomada-routine` の `.claude/skills/anki-vocabulary/scripts/config.py` と
  `validate_phrase_cards.py`（2026-09-11 にメインセッションが直接読んで確認）。
  同 config.py は、8 語の WARN が運用で過半数のカードに出るなら落とす、という
  条件を併記している。
- 却下した案: 意味説明を独立して 20 語以下に縛る — このアプリは意味説明と
  穴埋め文を別の項目で持つので項目の分け方には合うが、参照実装より厳しく、
  実績のない値になる。
- 却下した案: もっと短くする（意味説明 + 穴埋め 30 語、例文 10 語） — さっと
  読めるが根拠のない値で、不自然に削った文が増える恐れがある。
- 注: 答えの語の上限 5 語は、将来の熟語・フレーズのための値である。最初の版の
  答えは 1 語。

## 2026-09-11 残る小さい未決は、発音だけここで決め、残りはプロダクト側リポジトリに送る

- 決定: 発音（音声・発音記号）は、最初の版のカードに持たせない。次の 6 つは
  braintrust では決めず、プロダクト側リポジトリで決める（handoff.md の
  「決まっていないこと」に書く）。新規カードの 1 日上限 N の初期値、日次カーブの
  持ち方（スナップショットか、履歴から再計算か）、定着区分と節目の演出を足す
  時期、FSRS オプティマイザを使い始める時期、将来のライブラリ化に備えた分離の
  度合い、熟語・フレーズの同一性キー。
- 理由: 発音はカードのスキーマに関わるので、ここで決めておく。無料で再配布
  できる発音音声は実質存在せず（`research/lexical-data-sources.md`、2026-09-07
  時点）、中核の体験（意味から語を引き出す）にも要らない。残りの 6 つは、実装や
  使用実績がないと根拠のある値にならない。ライブラリ化については、第 3 部
  2026-09-07 の「アプリのロジックから切り離せる形でアプリ内に置く」が制約として
  残る。
- 却下した案: 発音もプロダクト側に送る — ここで決める数は減るが、カードの
  スキーマに発音の欄を作るかが宙に浮く。
- 却下した案: 6 つも含めて全部ここで決める — handoff の残る論点は減るが、N や
  カーブの持ち方が根拠のない値になり、どうせ実装で変わる。

## 2026-09-11 最初の 90 枚が一巡する頃（約 4 週間後）に、Anki に足した差分が使われたかで続けるかを判定する。使われていなければ Anki に戻る

- 決定: 最初の 90 枚前後が一巡する頃（使い始めて約 4 週間）に振り返る。範囲の
  絞り込みを一度も変えていない、または進捗の表示が続ける理由になっていない
  なら、自前アプリをやめて Anki の Vocabulary デッキに戻る。続かなかったこと
  自体は判定に使わない。戻るときは、カード JSON を CSV にして Anki に取り込む。
- 理由: 自前アプリを作る理由は、Anki に足す 3 つ（見た目と操作、目的・
  トピック別の絞り込み、進捗と達成感）に限られる（第 4 部 2026-09-10）。
  そのうえ Anki も統計画面で、デッキや検索で絞った「推定総知識量」を出せる
  （第 5 部 2026-09-11）。差が薄い以上、判定は差そのものが使われたかで行う。
  続かないことは Anki に戻っても解決しないので、判定材料にならない。カード
  データ（生成 Skill・Lint・点検の成果物）は Anki に移せるので、戻っても
  無駄になるのはアプリ本体だけである。
- 却下した案: 続いたかで判定する（4 週間のうち週 5 日以上開けなければ戻る）
  — 分かりやすいが、続かない原因がアプリにあるのか自分にあるのかを分けられ
  ない。Anki に戻っても同じことが起きうる。
- 却下した案: 撤退の条件を置かない — 個人用で実装の費用も小さいが、handoff に
  「何が効かなければ戻るか」が書けず、前提の検証が保留のまま残る。
- 位置づけ: 作る方向（第 4 部 2026-09-10）を蒸し返すものではない。
  `open-questions.md`「そもそも作るか」は検証待ちのまま、判定の条件と時期が
  ここで決まった。

## 2026-09-11 english-vocab-app を実装へ進める

- 決定: 実装へ進む。`handoff.md` を書き、`docs/plan/README.md` の状態を
  「実装へ」にする。引き継ぎ先は `nextjs-app-template` から切り出す新規
  リポジトリで、名前は未定。
- 理由: 生成 Skill の最初の 1 回を回すのに要る決定（トピック、生成と点検の
  設計、Lint の閾値）がそろった。残る未決は、実装や使用実績がないと根拠の
  ある値にならないものだけである（本部の「残る小さい未決は…」）。
- 却下した案: 保留する — 待っても braintrust の中で得られる材料がない。

## 第 7 部: カードを必要な分だけ作り切る（2026-09-19）

プロトタイプ（english-vocab-app、カード 75 枚）が動いた後のセッション。カードを今後
どう作るかを扱った。材料は別セッションの調査 `research/card-completion-strategy.md`
（2026-09-19）。メインセッション（Opus）がその調査を分析し、選択肢と推奨を提示した。
オーナーは会話と AskUserQuestion で決めた。推奨と違う選択をしたものは、各エントリに
その旨を書いた。

## 2026-09-19 カードは必要な分を作り切る。15 トピックへの拡張も今すぐ始める

- 決定: カードの生成を「足りない範囲を回すたびに埋める」から「必要な分を作り切る」に
  切り替える。最初の 3 トピック（environment / education / health）の判定を待たずに、
  15 トピックすべてを今回の生成の範囲に入れる。作った枚数を何日で学び切れるか
  （新規 10 枚 / 日で 1,500 枚なら約 150 日）は基準にしない。
- 理由: オーナーの言葉「もう必要な分は作り切る」「150日の基準は気にしない」
  「とりあえずちょっと作って終わり、みたいなのは面倒なので避けたい」。
- 却下した案: 1 回目の無人実行は #43 の語（約 21 語）だけで一度止め、品質を対話で
  作った #42 と比べてから広げる（メインセッションの推奨だった） — 無人で作る品質を
  少ない枚数で先に確かめられるが、オーナーは刻んで止める手間を避けた。
- 却下した案: 3 トピックでの判定（約 4 週間後）を待ってから広げる — 判定で Anki に
  戻る場合の点検の手間を節約できるが、カードは CSV で Anki に移せるので、作った分は
  無駄にならない。
- 無効になる過去の決定:
  - 第 4 部 2026-09-10「カードデータは生成用の Skill を回して足りない範囲を埋め…」の
    「一度に全量を生成しない」の部分。
- 位置づけ: 第 6 部 2026-09-11 の約 4 週間後の判定は、アプリを続けるかどうかの判定と
  して残す。記録上、もともとトピックを広げる条件ではない。
- 承知した上での割り切り: 無人で作るカードの品質が、対話で作った #42 と同等かは確かめ
  ないまま全量を作る。品質は 4 本の点検と、覆した判定の件数の報告で見張る。

## 2026-09-19 CEFR レベルごとに枚数の枠を決めて埋める区切り方は採らない。生成の単位はトピック、順番は論証語の層 → トピック固有の層

- 決定: 生成の単位はトピックとし、レベルは各カードの属性にとどめる（範囲は目標 IELTS
  7.0 に合わせて B2〜C1）。先にトピックをまたぐ論証語（動詞・評価の形容詞・ヘッジの
  副詞など）の層を作り、その後に 15 トピックそれぞれの固有の語の層を作る。
- 理由: オーナーの言葉「レベルあたり何枚、という区切り方で作り始めるのはなんか違う」。
  IELTS の採点が 6 と 7 を分けるのは語の数ではなく、あまり一般的でない語・コロケー
  ション・精度である（IELTS Writing Band Descriptors、2023-05）。カードにすべきなのは
  「知っているが書けない B2〜C1 の語」で、レベルの枠では切り出せない。論証語を先に
  作るのは、トピックごとに回すと同じ語が全トピックで提案され、衝突とタグ追記ばかりに
  なるため（`research/card-completion-strategy.md` Q3）。
- 却下した案: CEFR レベル × トピックの各マスに定数（例: 30 枚）を置く（メイン
  セッションが最初に出した案） — 止め時は明確だが、何が必要かに答えていない。
- 無効になる過去の決定: 第 4 部 2026-09-10「生成 Skill は IELTS のトピック単位と目標
  スコア単位の両方で回す（方向づけ）」のうち、目標スコア単位の部分。

## 2026-09-19 完了の条件は枚数ではなく飽和とする。被覆率の物差しは同じ Claude の独立した実行が書き、止め時の目安として扱う

- 決定: トピックごとに次の 2 つを測る。(a) 候補を角度を変えて列挙し直したとき、既存
  カード・タグ追記・台帳のどれにも当たらない新しいキーの割合。2 周続けて 10% 未満なら
  生成を止める。(b) カードを書いたのとは別の実行に、そのトピックの Writing Task 2 /
  Speaking Part 3 の問いを新しく作らせて band 7 の答えを書かせ、そこに出る B2 以上の
  内容語のうち、デッキか台帳にあるものの割合。90% 以上が 2 回続けばそのトピックは
  完了とする。閾値は調査が置いた出発点で、運用しながら変える。
- 理由: 外部の語彙リストを物差しに使わない（第 2 部 2026-09-07）以上、網羅度は自分で
  作った独立の標本との一致率でしか測れない。(b) は「使える語」を直接測れる。
- 却下した案: band 7 の答えを別ベンダーのモデル（Codex CLI など）に書かせる（メイン
  セッションの推奨だった） — カードを書くモデルと偏りがそろわず、被覆率が甘く出に
  くい。オーナーは手間の少ない同じ Claude を選んだ。
- 却下した案: 語数の目標を決める — 研究の数字（Drummond 2018 の 6.5 → 7.0 で平均約
  1,300 語家族など）は単位も母集団もそろわず、オーナー個人の不足分を言えない。
- 承知した上での割り切り: 同じモデル系列が書く答えなので、被覆率は高めに出る。(b) は
  完了の証明ではなく、止め時の目安として扱う。

## 2026-09-19 落とした語の台帳と、カードの層のフィールドを、生成を始める前に用意する

- 決定: 生成の前に次を用意する。落とした語（易しすぎ・価値が低い・対抗語に負けた等）
  の台帳をリポジトリの `data/` の下に置き、台帳にあるキーのカードを Lint で弾く。
  カードのスキーマに層（論証語 / トピック固有）のフィールドを足し、生成時に書かせる。
  枚数・品詞・レベル・層・飽和の指標を出す集計スクリプトを用意する。
- 理由: オーナーの言葉「先に作っておかないと作成およびチェックに困ることはやっちゃい
  たい」。台帳がないと、落とした語が列挙のたびに「新しい」として戻り、飽和が測れない。
  新規カードの出題順は「レベル順 → id のハッシュ順」で作った順番を反映しないため、層を
  後から 1,500 枚に付け直すと再判定の手戻りになる。
- 却下した案: 台帳を `/goal` の状態ディレクトリに置く — 実行ごとに消え、次の生成で
  使えない。
- 注: 出題順を層で並べるアプリ側の変更は、生成の後に回す。

## 2026-09-19 既知語を事前に落とす工程は入れない

- 決定: 生成した候補をオーナーが見て「もう使える語」を落とす工程は設けない。既知語は
  学習で Easy を押して流し、多すぎると感じたら 1 日の新規カード数を上げる。
- 理由: 見出し語の一覧で「知っている」と判定するのは再認のテストで、カードが鍛える
  能動的想起とは別物である。事前に落とすと、狙うべき「知っているが書けない語」から
  削られる（`research/card-completion-strategy.md` Q4）。Easy で流す費用は、ts-fsrs の
  既定値で 1 年目に復習 3 回程度（8 日 → 66 日 → 397 日）と、新規カード枠 1 つ。

## 2026-09-19 カード生成の無人実行を条件付きで認める。コミットはローカルのブランチにバッチごと

- 決定: `/goal` による無人のカード生成を認める。条件: 専用のローカルブランチの上だけ
  で作業し、バッチごとにコミットする。push・プルリクエスト・マージはしない。4 本の点検
  （blind / perspective / adjudication / level-and-selection）を毎回すべて回し、覆した
  判定はすべて理由付きで記録する。取り込むかはオーナーが自分のプルリクエストで決める。
- 理由: english-vocab-app が無人の生成を禁じた理由は「shipping-issues が承認なしで
  マージする」ことと「オーナーが全カードを読む」ことで、後者は #44 で取り下げられ、
  前者はマージしない実行には当たらない。数十バッチを回す実行で途中のコミットがないと、
  途中で失敗したときに切り戻せない。
- 却下した案: 作業ツリーの変更までで止め、コミットは人間が行う — english-vocab-app の
  AGENTS.md「Commit, push, and pull request always need a human」をそのまま守れるが、
  未コミットの変更が数十バッチ分溜まる。
- 影響: english-vocab-app 側で、`building-the-vocab-app/references/screens-and-pipeline.md`
  の無人実行を禁じる記述と点検「2 本」の記述、AGENTS.md のコミットの規則に、カード
  生成ブランチの例外を足す（プロダクト側で直す）。

## 2026-09-19 品詞比率の目標値は、デッキ全体で動詞 25% 以上・名詞 50% 以下・副詞 1 枚以上。論証語の層では動詞・形容詞・副詞の合計 60% 以上

- 決定: 上記を目標値とし、集計で確かめる。
- 理由: 最初のバッチは名詞 45・形容詞 20・動詞 10 で、Writing Task 2 / Speaking Part 3
  で得点になる精確な動詞が足りない（#43）。値は調査の案をオーナーがそのまま選んだ。
- 却下した案: 目標値を持たず、集計だけ出す — 最初のバッチの偏りは、どのルールも数え
  なかったから起きた（#44）。

## 2026-09-19 今回の範囲は単語だけ。熟語・コロケーションは入れない。#43 は閉じて中身を移す

- 決定: 品詞は今の 4 種類（noun / verb / adjective / adverb）に閉じたまま、単語だけを
  作る。#43 は閉じ、そこで決まっていた語は候補に、議論で落とした語は台帳に移す。
- 理由: 熟語の同一性キーは未決（プロダクト側）で、スキーマの変更を伴う。単語を作り
  切ることは無駄にならない。#43 はオーナーの言葉「#43は一回クローズしちゃう」。
- 承知した上での割り切り: 調査で引いた Read & Nation (2006)・Banerjee ら (2007) は、
  band 7 付近では低頻度の単語の数より、定型表現・コロケーションの比重が増すと述べる。
  単語を作り切っても、それだけで 7.0 に効くとは期待しない。

## 2026-09-19 却下（Anki に勝てない。スマホ・オフラインを数えていなかった）

- 決定: english-vocab-app は没にする。状態を「実装へ」から「却下」に戻す。実装は続けない。
  プロダクト側リポジトリ `~/ghq/github.com/tomada1114/english-vocab-app` はオーナーが
  自分で削除する。**作った 111 枚のカードも Anki には写さない。**
- 理由: オーナーの明示（2026-09-19）「既存の Anki の方がスマホでも使いやすかったり、
  オフラインでも使えたりと、やはり勝てない」。**実装がかなり進んだうえでの判断である**
  （カード 111 枚、画面 3 つ（ホーム・学習・セッション詳細）、FSRS、SQLite の復習ログ、
  日次の進捗カーブ、飽和判定つきの `cards:stats`、生成・点検の Skill 群）。
- 理由（同日の検討で確かめた事実）: 第 4 部 2026-09-10 で決めた「Anki に足す 3 つ」は、
  調べた結果ほとんど Anki 側に既にあった。
  - **目的・トピック別の絞り込み**: フィルタードデッキは任意の検索文字列で作れ
    （"create filtered decks with arbitrary search strings"）、FSRS のスケジューリングも
    保たれる（"Anki will return cards to their home decks with altered scheduling, based on
    your performance in the filtered deck"）。タグ `ielts` / `environment` / `c1` を振れば
    `tag:ielts (tag:environment or tag:health) tag:c1` で済む。なお、目的もトピックも語が
    複数持つ属性なので、デッキではなくタグが正しい機構である（デッキは 1 カード 1 つ）。
    出典: https://docs.ankiweb.net/filtered-decks.html （2026-09-19 に確認）
  - **進捗と達成感**: 統計画面は任意の検索でスコープできる（"You can also display graphs
    for arbitrary searches by adding filters in the search box at the top (2.1.28+)"）。
    Estimated total knowledge は "multiplying average retrievability by the number of cards
    that have been reviewed at least once" で、第 5 部 2026-09-11 で決めた「今覚えている
    語数」と定義が一致する。残る差は「別画面か学習の流れの中か」と「セッション終了時の
    差分」だけ。出典: https://docs.ankiweb.net/stats.html （2026-09-19 に確認）
  - **見た目と操作**: カードテンプレートは HTML/CSS/JS なのでカード面は自由。変えられない
    のは復習画面の外枠だけ。
- 理由（今回の失敗の核）: **「足すもの」を 3 つ数えたが、「引くもの」を数えていなかった。**
  第 4 部 2026-09-10 で「ローカルで動かす。デプロイは後で考える」と決めた時点で、Anki が
  持つスマホ（AnkiDroid / AnkiMobile）・無料同期（AnkiWeb）・オフラインを同時に捨てていた。
  この引き算は overview.md にも experience.md にも一度も書かれていない。
- 却下した案: デプロイして PWA 化する — スマホとオフラインは技術的には埋まる。ただし Anki が
  最初から持っているものに追いつくだけの追加作業であり、オーナーは選ばなかった（2026-09-19 に
  この選択肢を提示したうえでの判断）。
- 却下した案: カード JSON を CSV にして Anki に取り込む — 111 枚と生成・点検パイプラインの
  成果はそのまま生き、目的・トピック・CEFR はタグに落ちる。オーナーは写さないことを選んだ。
  **これにより第 5 部 2026-09-11「最初の 90 枚が一巡する頃（約 4 週間後）に…使われて
  いなければ Anki に戻る」の「戻るときは、カード JSON を CSV にして Anki に取り込む」は
  無効になる。**
- 却下した案: 4 週間判定を待つ（第 5 部 2026-09-11） — 判定材料（範囲の絞り込みを実際に
  変えたか、進捗の表示が続ける理由になったか）を実地で得られるが、スマホ・オフラインで
  勝てないことは判定を待たずに決まっている。
- 承知した上での割り切り: カード 111 枚、画面 3 つ、生成・点検の Skill 群、マージ済みの
  プルリクエスト 10 本前後を捨てる。カードを写さないので、素材の資産も残らない。
- 申し送り: **既存ツールに対して自前アプリを作ると決めるときは、「足すもの」と同じ丁寧さで
  「引くもの」を列挙すること。** 自前アプリは既存ツールの土台（モバイル、同期、オフライン、
  エコシステム、アドオン）を一緒に捨てる。今回は足す 3 つだけを数え、そのうち 2 つは調べたら
  既存側にあり、引く 3 つは最後まで数えられていなかった。これは english-opinion-trainer の
  「部品ごとの決定が積み上がっても、体験としてのまとまりは別に設計しないと出てこない」
  （2026-09-15）と並ぶ教訓であり、次に「既存 X の代替を自前で」を考えるときは両方を読むこと。
- 申し送り（解けていない痛み）: overview.md が挙げた元の痛みのうち、**産出側が苦しい /
  覚えたはずなのに使えない / 単語だけではマッピングができない**は、このアイデアでは一度も
  解かれていない。これらを扱っていた自由記述 + LLM 添削は第 4 部 2026-09-10「アプリに AI を
  組み込まない」で消え、残った 3 点はどれも当たっていなかった。痛み自体は消えていない。
- 残るもの: 決定・調査（競合 9 製品、`research/` 10 本）・open-questions.md はそのまま残す。
